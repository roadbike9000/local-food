---
baseline_commit: d0fae663f691ffc4f60397d4d30c59166f1f5749
---

# Story 6.1: Pickup-slot times are interpreted in the vendor's own timezone

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a vendor,
I want the pickup times I enter to be understood as *my* local time, not the browser's,
so that a slot I create is never silently off by however many hours separate my timezone from whoever's device submitted the form.

## Acceptance Criteria

1. `Vendor` gains a `timezone` column (IANA timezone identifier, e.g. `"America/New_York"`, non-nullable with a schema default) — every existing vendor row gets that default value via the migration itself (a plain `@default(...)` on a new NOT NULL column backfills automatically; this is not the two-step nullable-then-backfill shape Story 1.2's `stockQuantity` needed, since every vendor gets the identical default value, not one derived per-row). No admin/vendor-facing UI to *change* it is required by this story — just the column existing and being used.
2. When a vendor submits `AddSlotForm.tsx`'s `startsAt`/`endsAt` `datetime-local` inputs, the entered wall-clock digits are interpreted as being in that vendor's `timezone`, not the submitting browser's local timezone, before being converted to the absolute UTC instant sent to `POST /api/pickup-slots` and stored in `PickupSlot.startsAt`/`endsAt`. This is the story's core deliverable: closes the exact gap the original scenario review named — someone in a different timezone than the vendor's actual business location can no longer silently create a slot that's off by the offset between the two.
3. `POST /api/pickup-slots`'s wire format is unchanged — it still receives and validates an absolute ISO UTC instant (`CreateSlotSchema`'s existing `z.string().datetime({ offset: true })` fields), and its existing refines (`endsAt > startsAt`, `startsAt` not in the past — Story 5.2) are unaffected and need no code change, since both already compare absolute instants and are timezone-agnostic by construction. Confirm this holds with a real test rather than assuming; note explicitly in Completion Notes if a gap is found instead.
4. The storefront's "Next pickup" banner (`vendors/[slug]/page.tsx`) and the vendor dashboard's own pickup-slots listing (`dashboard/pickups/page.tsx`) both display pickup times in the vendor's configured `timezone` — not the viewing browser's local timezone (the current, unstated default) — since pickup happens at the vendor's physical location regardless of which timezone the viewer is browsing from.
5. `AddSlotForm.tsx`'s `startsAt` input's native `min` attribute (added in Story 5.2) continues to reflect "now" correctly — computed in the vendor's configured timezone, not the browser's, so the native picker's own lower bound agrees with what the server will actually accept.

*(FR17.)*

## Tasks / Subtasks

- [x] Task 1: `Vendor.timezone` schema migration (AC #1)
  - [x] `prisma/schema.prisma` — add `timezone String @default("America/New_York")` to the `Vendor` model. Pick the default deliberately (this codebase's existing vendors/seed data give no explicit signal either way — `America/New_York` is a reasonable single-market assumption for a first cut, but this is a real judgment call, not a mechanical one) and say what you picked and why in Completion Notes.
  - [x] Run `npm run prisma:migrate` — confirm via `npx prisma studio` or a direct query that every existing `Vendor` row picked up the default value, not `NULL`. No hand-written backfill script needed — this is the simple case (identical default for every row), unlike Story 1.2's `stockQuantity`/`lowStockThreshold` migration, which needed per-row derivation from `isAvailable` and is not a pattern to copy here.
  - [x] `docs/data-models.md` — add the new `timezone` row to the `Vendor` table (matches every other schema field's documentation precedent in that file).

- [x] Task 2: New `src/lib/timezone.ts` module — wall-clock ⇄ UTC conversion for an arbitrary IANA zone (AC #2, #5)
  - [x] **This is the highest-risk part of this story — read this task in full before starting, and do not hand-roll a simpler-looking version.** No date library (`date-fns`/`luxon`/`dayjs`/`date-fns-tz`) exists in this codebase's `package.json` today (confirmed by grep during story creation) — adding one is a legitimate option (`date-fns-tz` is the natural minimal fit if picked), but the codebase's existing pattern for every other date need (`formatPickupWindow`, `toDatetimeLocalValue`) is hand-rolled `Intl`-based code with no dependency. If implementing by hand, this reference algorithm is the standard technique for wall-clock→UTC-in-arbitrary-zone conversion using only native `Intl` (verify it, don't assume it's correct as pasted):
    ```ts
    // Converts a "YYYY-MM-DDTHH:mm" wall-clock string (no offset info, as
    // produced by a datetime-local input's .value) interpreted as being in
    // `timeZone` into the UTC instant it actually represents.
    export function zonedWallTimeToUtc(wallTime: string, timeZone: string): Date {
      const naiveUtc = new Date(`${wallTime}:00Z`); // treat the digits as if UTC
      const dtf = new Intl.DateTimeFormat("en-US", {
        timeZone,
        year: "numeric", month: "2-digit", day: "2-digit",
        hour: "2-digit", minute: "2-digit", second: "2-digit",
        hour12: false,
      });
      const parts = Object.fromEntries(dtf.formatToParts(naiveUtc).map((p) => [p.type, p.value]));
      // What naiveUtc actually displays as, in the target zone:
      const asDisplayedInZone = Date.UTC(
        Number(parts.year), Number(parts.month) - 1, Number(parts.day),
        Number(parts.hour) % 24, Number(parts.minute), Number(parts.second),
      );
      const offsetMs = asDisplayedInZone - naiveUtc.getTime();
      return new Date(naiveUtc.getTime() - offsetMs);
    }
    ```
  - [x] Also add the inverse-ish direction for AC #5 / display use: a function that formats a UTC `Date` as a `"YYYY-MM-DDTHH:mm"` wall-clock string *in a given timezone* (simpler than the above — no offset math needed, `Intl.DateTimeFormat(...).formatToParts()` already returns the target zone's wall-clock components directly for a given instant). `AddSlotForm.tsx`'s existing `toDatetimeLocalValue()` does this today but hardcodes the browser's own zone (via `d.getFullYear()` etc., not `Intl`) — either generalize it to accept a `timeZone` param and move it into `src/lib/timezone.ts`, or add a sibling function; dev's call which, but don't leave two divergent implementations of the same formatting logic.
  - [x] **Mandatory Vitest coverage, not optional:** at minimum — (a) a non-DST-boundary date in a zone behind UTC (e.g. `America/New_York`) round-trips correctly; (b) a zone ahead of UTC round-trips correctly; (c) **at least one real DST-transition-boundary case** (e.g. a wall-clock time on the day of `America/New_York`'s spring-forward or fall-back transition) — this is exactly the class of bug that looks correct on every "normal" date and silently breaks twice a year; skipping this case is not acceptable coverage for this module. Use a fixed real date for the DST case (look up the actual 2026 US DST transition dates), not a relative one, since the whole point is pinning behavior at a known boundary.

- [x] Task 3: Thread `Vendor.timezone` through slot creation (AC #2, #5)
  - [x] `src/app/dashboard/pickups/page.tsx` already fetches `vendor` (a Server Component, `getCurrentVendor()`) — pass `timezone={vendor.timezone}` as a new prop to `<AddSlotForm />` (currently rendered with no props).
  - [x] `AddSlotForm.tsx` — accept the new `timezone: string` prop. Replace `new Date(startsAtLocal)` / `new Date(endsAtLocal)` (currently interpreted in the *browser's* local zone) with `zonedWallTimeToUtc(startsAtLocal, timezone)` / `zonedWallTimeToUtc(endsAtLocal, timezone)` from the new module. The rest of `handleSubmit` (validation, `.toISOString()`, the POST body shape) is unchanged — this only changes *how* the `Date` objects are constructed from the raw input strings, matching AC #3's "wire format unchanged" requirement.
  - [x] `AddSlotForm.tsx`'s `min={toDatetimeLocalValue(new Date())}` (Story 5.2) — replace with the vendor-timezone-aware equivalent from Task 2, so the native picker's lower bound is computed in the vendor's configured zone, not the browser's (AC #5).
  - [x] **Known, accepted UX limitation, not a gap to close in this story:** the `datetime-local` input widget itself is always rendered and labeled by the browser in the browser's own local timezone — there's no way to make the widget's own chrome say "this is Eastern time" if the vendor's browser is set to Pacific. This story treats the digits the vendor types as vendor-timezone digits regardless of what the widget's surrounding UI implies, per Jeff's explicit 2026-08-26 decision (`deferred-work.md`). A visible "you're entering times in {vendor.timezone}" hint next to the form would be a reasonable follow-up UX improvement, but is not required by any AC here — don't scope-creep into it.

- [x] Task 4: Display pickup times in the vendor's own timezone (AC #4)
  - [x] `src/lib/utils.ts`'s `formatPickupWindow(startsAt: Date, endsAt: Date)` — add a required third parameter, `timeZone: string`, and pass `{ timeZone, ... }` into both `toLocaleDateString`/`toLocaleTimeString` calls' options objects (native `Intl`/`Intl`-backed `Date` methods already support a `timeZone` option — no new dependency needed for this half, this is the simple direction of the conversion problem, unlike Task 2's).
  - [x] Update both existing call sites to pass it: `src/app/vendors/[slug]/page.tsx` (already has `vendor.timezone` in scope from its Prisma query) and `src/app/dashboard/pickups/page.tsx` (same). This is a signature-breaking change to a shared utility — grep for every call site before considering this task done, don't rely on TypeScript alone to catch a missed one if a call site was ever wrapped in a way that erases the type error.
  - [x] **Not anticipated by this task's original two call sites — found via the grep this task itself demands:** `src/app/cart/page.tsx` (the customer-facing checkout page) has 2 more `formatPickupWindow` call sites, displaying pickup windows to the *customer*, not the vendor. AC #4's own rationale ("pickup happens at the vendor's physical location regardless of which timezone the viewer is browsing from") applies identically here, so these needed the same fix, not an exception. `GET /api/vendors/[vendorId]/pickup-slots` (the route `/cart` already fetches slots from) now also returns `timezone` alongside `slots` — a real, documented API-contract addition (`docs/api-contracts.md` updated), not silently absorbed.

- [x] Task 5: Tests (AC #1-#5)
  - [x] Task 2's own Vitest coverage (see above) is the primary correctness proof for the conversion logic itself — don't substitute e2e coverage for it.
  - [x] New Vitest cases for `formatPickupWindow()` (`src/lib/utils.test.ts`) — same instant, two different `timeZone` values, confirms the displayed string actually differs (proves the parameter is wired through, not silently ignored).
  - [x] Extend `tests/dashboard.spec.ts`'s existing `AddSlotForm` e2e tests (`"vendor can add a new pickup slot"`) — re-ran after Task 3 landed: still passes. Confirmed this is a weak regression check on its own (asserts only that the location text becomes visible, not the exact stored instant) — that gap is exactly why the new dedicated test below exists.
  - [x] New Playwright test (`tests/dashboard.spec.ts`) proving AC #2 end-to-end — **implemented differently than originally planned, with reasoning:** a second throwaway `createTestVendor()` fixture can't drive `AddSlotForm` through real UI interaction, since no Clerk identity exists for one (the form only renders for whichever vendor the single seeded E2E session is signed in as). Instead, the test temporarily mutates `corner-sourdough`'s own `timezone` to `"Asia/Tokyo"` (fixed UTC+9, no DST — deterministic regardless of season or the runner's own machine zone), submits a fixed future wall-clock time through the real form, and asserts the stored `PickupSlot.startsAt` against an independently-computed expected UTC instant (not by calling this app's own `zonedWallTimeToUtc` — a genuine external proof, not a tautology). Restores the original timezone in `finally`, same discipline as this file's existing location-fixture cleanup pattern.
  - [x] New Playwright assertion that pickup times render in `vendor.timezone` (AC #4) — **narrowed scope, with reasoning:** the same new test also asserts the dashboard's slot listing shows the correct Asia/Tokyo-formatted string. The storefront's "Next pickup" banner was *not* separately covered — it only shows `pickupSlots[0]` (soonest-first), and this codebase's seeded vendor already has an earlier slot, so exercising the banner for *this* new slot specifically would need extra setup (clearing/reordering existing slots) disproportionate to what's actually at risk here: `formatPickupWindow()`'s own Vitest coverage plus TypeScript's required-parameter enforcement already give strong confidence the banner call site (identical one-line change to the dashboard listing's) is correct. Flagging this as a deliberate, reasoned scope call, not an oversight.

- [x] Task 6: Test helpers and docs sync (housekeeping, matches established precedent)
  - [x] `tests/helpers/db.ts`'s `createTestVendor()` — added a `timezone` override to its `overrides` type (mirrors the existing `deletedAt`/`deletedByAdminId` override pattern). Not used by the dedicated timezone test in the end (see Task 5's note above — that test mutates the seeded vendor directly instead), but added anyway since it's a natural, low-cost extension of an existing pattern and may be useful to a future test.
  - [x] `docs/data-models.md` — the new `Vendor.timezone` row (Task 1) added.
  - [x] `docs/api-contracts.md` — `POST /api/pickup-slots`'s comment now explains the wire format is unchanged (still an absolute ISO instant) but the dashboard client now computes that instant relative to the vendor's own timezone, not the browser's. Also updated `GET /api/vendors/[vendorId]/pickup-slots`'s contract for a real, unplanned addition — see Task 4's note about the `cart/page.tsx` call sites found via grep.

### Review Findings

3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor — all three on Opus, per Jeff's request) against `git diff d0fae663f691ffc4f60397d4d30c59166f1f5749..atdd-6-1-pickup-slot-timezone-2026-08-27` (the ATDD scaffold + implementation, 892 diff lines). All three layers independently found and reproduced the same core defect by execution, not by inspection — cross-verified directly (not just trusted) before acting.

- [x] [Review][Patch] **`zonedWallTimeToUtc()` computed the wrong UTC instant for a 4-5 hour window on every DST transition day, in the schema's own default zone (`America/New_York`)** — the single-pass algorithm sampled the zone's offset *at the naive-UTC instant* (the wall-clock digits reinterpreted as UTC) rather than at the true target instant, which can be several hours later. When a DST transition falls in that gap, the stale pre-transition offset gets applied. Verified independently by all three reviewers by execution against the real implementation, and re-verified again here via a round-trip check and against Python's `zoneinfo`: wrong for wall-clock `03:00`-`06:59` on the 2026 spring-forward day and `02:00`-`05:59` on the fall-back day (not just the transition hour itself) — exactly the failure mode this story's own Dev Notes named as the one way the work could "look done... and still silently corrupt data for two days a year." The original DST test cases all sampled `09:00`, which sits outside both broken windows in both directions — true, satisfied the letter of "at least one real DST-transition-boundary case" while missing the substance. **Fixed:** two-pass offset resolution (resample the offset at the first pass's candidate result, then re-derive) — `src/lib/timezone.ts`. **New tests added, each independently cross-checked against `zoneinfo`:** 8 cases pinned inside the previously-broken windows (4 spring-forward, 4 fall-back) — all pass against the fixed implementation, all would fail against the original.
- [x] [Review][Patch] **A wall-clock time that never exists (the hour skipped by spring-forward) was silently accepted and converted to a plausible-looking but arbitrary instant** — no offset arithmetic can produce a "correct" answer for a time that never happened; the only honest behavior is to reject it. **Fixed:** `zonedWallTimeToUtc()` now round-trips its own result and throws `InvalidWallTimeError` if it doesn't match the input — this single check also subsumes the malformed/empty-string case below. **New test:** `"2026-03-08T02:30"` (inside the skipped hour) throws.
- [x] [Review][Patch] **An ambiguous wall-clock time (the hour repeated by fall-back) had an undocumented, unpinned resolution policy** — `"2026-11-01T01:30"` occurs twice (once as EDT, once as EST an hour later); the implementation picks one but nothing declared which. **Fixed:** documented the policy (earlier/EDT occurrence) directly in a pinned test, verified against `zoneinfo`'s `fold=0` result.
- [x] [Review][Patch] **The claim that an empty/malformed input "throws a `RangeError`" was factually wrong, and the actual behavior was worse than the claim** — `new Date(":00Z")` (what an empty wall-time string produces) doesn't throw; it silently parses to `2000-01-01T00:00:00Z`. Verified by direct execution. **Fixed:** `zonedWallTimeToUtc()` now validates the input shape (regex) and rejects a non-parseable naive instant explicitly, both throwing `InvalidWallTimeError`, before any offset math runs.
- [x] [Review][Patch] **`AddSlotForm.tsx` called `zonedWallTimeToUtc()` *before* the `try` block that was meant to handle its failures** — a thrown error (e.g. from the malformed-input case above) left `submitting` stuck `true` with no error shown; the form went permanently dead. **Fixed:** both conversion calls moved inside a `try`/`catch`, which also let the separate shape-only regex guard be removed entirely — one validation point instead of two, and it now also catches the "shape-valid but semantically nonsense" case (e.g. month 13) the regex alone never could.
- [x] [Review][Patch] **`formatPickupWindow()` and `utcInstantToZonedDatetimeLocal()` had no defense against an invalid `Vendor.timezone` value** — `Vendor.timezone` is unvalidated free text with no write-side check anywhere in this codebase; a bad value would throw a raw `Intl` `RangeError`. Two of `formatPickupWindow()`'s four call sites are Server Components (`vendors/[slug]/page.tsx`, `dashboard/pickups/page.tsx`) — an uncaught throw there 500s the whole public storefront/dashboard route, not just one displayed time. **Fixed:** new `isValidTimeZone()` helper (constructs an `Intl.DateTimeFormat` and lets it validate — checking `Intl.supportedValuesOf("timeZone")` membership was tried first and rejected, since that list excludes `"UTC"` itself, which would have made the helper wrongly reject this codebase's own fallback value); both functions fall back to `"UTC"` for an invalid zone rather than throwing. `zonedWallTimeToUtc()` deliberately still throws on a bad zone — it's correctness-critical (produces the *stored* value) and its one caller now safely catches it, unlike the two display-path functions.
- [x] [Review][Patch] **`cart/page.tsx` cast the `GET /api/vendors/[vendorId]/pickup-slots` response's `timezone` field with no runtime check** — if the field were ever absent (stale cache, proxy, partial deploy), `timeZone: undefined` would reach `Intl`, which silently falls back to the *browser's* zone — reintroducing the exact bug this story exists to close, through the one new network hop it added. **Fixed:** runtime type + `isValidTimeZone()` check, falls back to `"UTC"`.
- [x] [Review][Patch] **`vendorTimezone` state in `cart/page.tsx` wasn't reset when `vendorId` changed** — switching vendors could briefly render the *previous* vendor's slots in the *previous* vendor's timezone until the new fetch resolved. **Fixed:** reset to `"UTC"` alongside the rest of the effect's state.
- [x] [Review][Patch] **`GET /api/vendors/[vendorId]/pickup-slots` awaited two independent queries sequentially** — no dependency between the vendor lookup and the slots query; needless added latency on a public per-cart-load endpoint. **Fixed:** `Promise.all`.
- [x] [Review][Patch] **The new end-to-end test hardcoded a specific future calendar date (`2026-12-15`)** — Story 5.2's past-`startsAt` rejection would have started failing this test the day that date passed, roughly 3.5 months of useful life for the story's one true proof of AC #2. **Fixed:** date now computed relative to "now" (`Date.now() + 200 days`); still safe against DST since `Asia/Tokyo` has none.
- [x] [Review][Patch] **The same test's `waitForResponse` didn't assert the response was actually successful** — a real `400` would still satisfy the wait, surfacing as a confusing visibility timeout later instead of a clear signal. **Fixed:** `expect(response.ok()).toBe(true)` added.
- [x] [Review][Patch] **Two other tests' comments in `tests/dashboard.spec.ts` still referenced `AddSlotForm.tsx`'s `toDatetimeLocalValue()`**, deleted by this story's own Task 3. **Fixed:** reworded to describe actual current behavior.
- [x] [Review][Patch] **`createTestVendor()`'s new `timezone` override used a different idiom (conditional spread) than its three siblings (`?? null`)**, for no reason — and was dead code (unused by the test that was originally planned to need it). **Fixed:** matched the established `?? undefined` idiom; kept as a natural extension of the existing override shape, not removed, since a future test may need it.
- [x] [Review][Patch] **`hour === "24"` (ICU's midnight-under-`hour12:false` quirk) was worked around two different ways in the same file** — a numeric `% 24` in one function, a string `=== "24"` ternary in the other. **Fixed:** `hourCycle: "h23"` on the shared format options removes the need for either.
- [x] [Review][Patch] **Dev Notes' own instruction to declare the discretionary `CreateVendorSchema.timezone` field's skip status "in Completion Notes" wasn't followed** — it was skipped (correctly, per the story's own discretionary framing) but only mentioned in `docs/data-models.md`, not Completion Notes as explicitly required. **Fixed:** added below.

- [x] [Review][Defer] **Every vendor now defaults to `America/New_York`, permanently, with no UI to change it** — a vendor genuinely located in a different timezone gets every slot silently stored/displayed an offset off, with no remedy in the product. This is a direct, foreseeable consequence of AC #1's explicit scope decision (no settings UI, single default) made when this story was created — not a code defect, but a real product-level consequence worth Jeff's awareness. Logged in `deferred-work.md` for a decision on next steps (settings UI, or explicitly accept).
- [x] [Review][Defer] **The migration retroactively changes how every pre-existing `PickupSlot` is *displayed*** — those rows were created under the old browser-local interpretation; nothing about their stored instant changed, but any vendor not actually in Eastern now sees (and shows customers) times shifted by the offset between their real zone and the new default. No backfill, audit, or vendor-facing notice exists. Same root cause as the item above — logged together in `deferred-work.md`.
- [x] [Review][Defer] **`formatPickupWindow()`'s displayed string carries no timezone label** (e.g. "5:00 PM EST") — now that the vendor's zone is decoupled from the viewer's own, a customer on `cart/page.tsx` has no indication the time shown isn't their own local clock. Real UX gap, but new scope beyond any stated AC — logged in `deferred-work.md`.
- [x] [Review][Defer] **AC #5's `min` attribute is a blocking native-validation constraint expressed in a zone the widget doesn't display**, layered on top of the story's own already-accepted "widget labelling" UX limitation — a vendor whose browser zone differs from their configured zone can see the picker reject a time that looks plainly future to them, with no explanation. The story explicitly declined a "you're entering times in {timezone}" hint as scope creep for the *labelling* case; this is arguably a step further since it's a *blocking* constraint, not just an unlabeled display. Logged in `deferred-work.md` for a decision on whether the accepted limitation should be revisited.
- [x] [Review][Defer] **The new e2e test mutates the shared seeded `corner-sourdough` vendor's timezone under `fullyParallel: true`** — serialized within `dashboard.spec.ts` itself, but other spec files run concurrently against the same vendor. No current assertion collides (verified: no other spec asserts on rendered pickup-window text), but a worker crash mid-test would skip the `finally` restore and leave the seeded vendor pinned to `Asia/Tokyo`, poisoning subsequent runs — the same class of drift this project has hit before. Accepted as a documented residual risk (the comment in the test itself now says so) rather than engineered away — a crash-safe global restore is disproportionate to a risk with no live consequence today.
- [x] [Review][Defer] **`GET /api/vendors/[vendorId]/pickup-slots` has no `deletedAt` filter on its vendor lookup** — pre-existing gap (the route never had one, even before this story), not introduced here; a soft-deleted vendor's `timezone` (and slots) are still served. Out of this story's scope.
- [x] [Review][Defer] **`Vendor.timezone` has no write-side validation anywhere** (no zod check in `CreateVendorSchema`, no DB constraint) — the read-side `isValidTimeZone()` fallback (patched above) protects every current display/conversion path, but a future write path (an eventual settings UI, a direct SQL edit) could still introduce garbage. Worth a shared validation point if/when a write path is ever added — not urgent today since none exists.
- [x] [Review][Dismiss] `formatToParts()`'s output assumed to contain every expected part key, with no explicit guard — theoretical only; no realistic trigger given real ICU implementations, and every part is already present in every verified test case. Not worth building distinct error handling for a scenario with no known reproduction path.
- [x] [Review][Dismiss] Regex historically claimed to reject a seconds-bearing `datetime-local` value — real HTML-spec behavior in general, but unreachable here: neither `startsAt` nor `endsAt` sets a `step` attribute, so the input's granularity is minutes-only and `.value` never includes seconds. Moot in any case once the regex was removed entirely (see the throw-outside-try fix above).
- [x] [Review][Dismiss] `endsAt` has no native `min` attribute at all (only `startsAt` does) — pre-existing asymmetry, already reviewed and accepted in Story 5.2's own deferred-work.md entry ("the existing JS `endsAt <= startsAt` guard already covers that case"). Not introduced or touched by this diff.

**Verification after patches:** `npx tsc --noEmit` clean; `npm run lint` clean; `npx vitest run` 123/123 (108 prior + 15 new: 8 broken-window cases, 1 nonexistent-time, 1 ambiguous-time, 2 malformed-input, 2 `isValidTimeZone`, 1 `formatPickupWindow` fallback); `npx playwright test` 137/137 (2 individually-confirmed pre-existing flaky failures during repeated full-suite runs — both on unrelated tests neither touching timezone code, both pass clean in isolation and on a subsequent full clean run, consistent with this project's already-documented Clerk-session-TTL-under-full-suite-runtime flakiness class); `npm run build` succeeds.

## Dev Notes

**This story's real complexity is entirely in Task 2's conversion math, not in the plumbing around it.** Every other task is straightforward parameter-threading. Getting `zonedWallTimeToUtc()` wrong (especially around a DST transition) is the one way this story can look done, pass a casual test pass, and still silently corrupt data for two days a year. Treat Task 2's Vitest coverage as this story's actual acceptance bar, not a formality alongside the "real" work.

**Why the wire format stays unchanged (AC #3) instead of moving zone-awareness server-side.** An alternative design would send the raw wall-clock string to the server and have `POST /api/pickup-slots` do the zone conversion (using `getCurrentVendor()`'s already-available `vendor.timezone`) instead of the client. That would match this codebase's general NFR2 "server-side is authoritative" preference more literally — but it would require moving the `endsAt > startsAt` / past-`startsAt` refines out of `CreateSlotSchema` (a pure function on strings today) and into the route, after the zone conversion happens, since a bare wall-clock string can't be compared for "is this in the past" without first knowing which zone it's in. That's a bigger, riskier restructuring of code Story 5.2 already hardened through review, for no behavioral difference the vendor would ever observe (the conversion result is byte-identical either way, since `vendor.timezone` doesn't change between the client fetching it and the client submitting the form — no trust boundary is actually being crossed by doing the math client-side here, unlike e.g. price or stock checks, which genuinely can be manipulated by a malicious client). Keep the conversion client-side, keep `CreateSlotSchema` exactly as Story 5.2 left it.

**`PickupSlot.capacity`'s enforcement (Story 5.1's deferred finding, resolved 2026-08-27 in `POST /api/checkout`) is unrelated to this story** — this story is about which timezone a slot's wall-clock input is interpreted in, not slot capacity. Don't conflate the two.

**No vendor-facing settings UI to change `Vendor.timezone` exists after this story ships**, per AC #1's explicit scope boundary — every vendor gets the Task 1 default and there is currently no way for a vendor or admin to change it through the app. If `POST /api/admin/vendors` (`CreateVendorSchema`) should optionally accept a `timezone` at vendor-creation time instead of always taking the schema default, that's a reasonable, low-risk addition (`Intl.supportedValuesOf("timeZone")` — a real runtime API — can validate an incoming IANA identifier without a new dependency, if this is picked up) but it is **discretionary**, not required by any AC here, matching Story 5.2's Task 3 precedent for optional scope. If skipped, say so in Completion Notes rather than silently omitting it.

**`formatPickupWindow()`'s new required `timeZone` parameter is a breaking signature change.** Grep for every call site (`src/app/vendors/[slug]/page.tsx`, `src/app/dashboard/pickups/page.tsx` are the two known today) before considering Task 4 done — don't assume those are the only two without checking, since a missed call site fails silently (wrong displayed time, not a compile error, if a default parameter value were added instead of a required one — don't add a default for this reason; a missing argument should be a type error, not a silent fallback to some incorrect implicit zone).

### Project Structure Notes

- **New:** `src/lib/timezone.ts` (Task 2's conversion functions).
- **Modified:** `prisma/schema.prisma` (`Vendor.timezone`), `src/lib/utils.ts` (`formatPickupWindow()` signature), `src/components/dashboard/AddSlotForm.tsx` (new `timezone` prop, conversion call sites), `src/app/dashboard/pickups/page.tsx` (pass `vendor.timezone` to both `AddSlotForm` and `formatPickupWindow`), `src/app/vendors/[slug]/page.tsx` (pass `vendor.timezone` to `formatPickupWindow`), `tests/helpers/db.ts` (`createTestVendor()` gains a `timezone` override), `tests/dashboard.spec.ts`, `docs/data-models.md`, `docs/api-contracts.md`.
- No changes needed to `src/app/api/pickup-slots/schema.ts`, `src/app/api/pickup-slots/route.ts`, `src/app/api/checkout/route.ts`, or `src/app/api/vendors/[vendorId]/pickup-slots/route.ts` — all four already operate on absolute instants and are timezone-agnostic by construction (AC #3). Confirm, don't assume.

### Testing Standards Summary

- Vitest for `src/lib/timezone.ts`'s pure conversion functions (Task 2) — this is the primary correctness proof for this story, including a real DST-boundary case.
- Vitest for `formatPickupWindow()`'s new `timeZone` parameter (Task 5).
- Playwright for the end-to-end vendor-creates-a-slot-in-their-own-zone path (Task 5) — the one test that actually exercises AC #2 against a non-default vendor timezone, not just the default the whole existing test suite happens to already use everywhere.
- No mocking — matches this codebase's established convention. `Intl`/`Date` timezone behavior is real runtime behavior, not something to stub.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 6.1] — story definition and ACs, and Epic 6's scope note.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — "scenario review with Jeff" entry (2026-08-24) and its 2026-08-26 decision note — the original scenario and Jeff's decision to add `Vendor.timezone` this story implements.
- [Source: _bmad-output/implementation-artifacts/5-2-pickup-slot-creation-rejects-a-past-start-time.md] — previous story touching this exact surface (read in full) — confirms `CreateSlotSchema`'s existing refines already compare absolute instants and are timezone-agnostic (Dev Notes: "this story's check is timezone-agnostic by construction"), the basis for this story's AC #3 and its decision to keep zone conversion client-side rather than restructure the schema.
- [Source: src/components/dashboard/AddSlotForm.tsx] — current form (read in full) — confirms today's `new Date(startsAtLocal)` browser-local interpretation and the existing `toDatetimeLocalValue()`/`min` attribute pattern this story extends.
- [Source: src/lib/utils.ts] — `formatPickupWindow()` (read in full) — confirms it takes no timezone parameter today and relies on the runtime's implicit default zone.
- [Source: src/app/dashboard/pickups/page.tsx, src/app/vendors/[slug]/page.tsx] — both call sites (read in full) — both already fetch a full `vendor` object via Prisma, confirming `vendor.timezone` will be available with zero new queries once Task 1 lands.
- [Source: src/app/api/pickup-slots/schema.ts, src/app/api/pickup-slots/route.ts, src/app/api/checkout/route.ts] — current schema/routes (read in full) — confirm all absolute-instant comparisons, supporting AC #3's "no code change needed here" claim.
- [Source: prisma/schema.prisma] — `Vendor` model (read in full) — confirms no existing timezone-adjacent field, and the exact shape (unique `slug`, nullable `clerkUserId`, etc.) `timezone` needs to fit alongside.
- [Source: tests/helpers/db.ts] — `createTestVendor()` (read in full) — confirms its existing `overrides` pattern to extend for Task 6.
- [Source: package.json] — confirms no date library (`date-fns`/`luxon`/`dayjs`/`date-fns-tz`) exists in this codebase today, the basis for Task 2's "flag this as an explicit choice" framing.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Task 2: implemented by hand with native `Intl` (no new dependency added) — the standard "treat the wall-clock digits as naive UTC, ask `Intl` what that instant displays as in the target zone, the difference is the offset" technique. All 11 ATDD-authored Vitest cases (including both 2026 US DST boundaries, independently cross-checked against Python's `zoneinfo` during the ATDD pass) flipped from red to green with zero test-file changes needed — the implementation was written to satisfy the pre-existing tests, not the other way around.
- Task 3: found and fixed a real regression risk while implementing, not caught by the reference algorithm alone — `zonedWallTimeToUtc`'s `Intl.DateTimeFormat.formatToParts()` throws a `RangeError` on a malformed/empty input, unlike the old plain `new Date(startsAtLocal)` parse, which just yields a gracefully-`NaN` `Date` that the existing `Number.isNaN` guard already catches. Fixed by validating the raw wall-clock strings' shape with a regex *before* calling `zonedWallTimeToUtc`, not after — moving the existing empty/invalid-input guard earlier rather than wrapping the new call in a try/catch.
- Task 4: grepping every `formatPickupWindow()` call site (as the task itself demands) surfaced 2 more in `src/app/cart/page.tsx` that neither the story file nor the epic anticipated — the customer-facing checkout page also displays pickup windows. Fixed in scope (AC #4's own rationale applies identically to a customer viewing a vendor's pickup time), which required extending `GET /api/vendors/[vendorId]/pickup-slots`'s response with a new `timezone` field (`cart/page.tsx` is a client component with no other way to learn the vendor's timezone) — a real, documented API-contract addition, not a silent scope expansion.
- Task 5: the story's own planned "second throwaway vendor fixture" design for the end-to-end AC #2 test doesn't work in this codebase — `createTestVendor()` fixtures have no Clerk identity, so nothing can sign in as one and drive `AddSlotForm` through real UI. Adapted to temporarily mutate the seeded `corner-sourdough` vendor's `timezone` (restored in `finally`) instead — same net proof, different mechanism. See the story's own Task 5 checkbox notes for full reasoning, and the same section for why the storefront "Next pickup" banner was deliberately left uncovered at the e2e level (unit + type-safety coverage judged sufficient for that one-line call site).
- `npx tsc --noEmit`, `npm run lint` — clean throughout, re-verified after every task.
- `npx vitest run` — 108/108 passed (96 pre-existing + 1 pre-existing test's coverage extended + 11 new).
- Full `npx playwright test` — 137/137 passed, zero skips, zero regressions.
- `npm run build` — succeeds.

### Completion Notes List

- `Vendor.timezone` (`String @default("America/New_York")`) added via `prisma/schema.prisma` + migration `20260827172107_add_vendor_timezone`. Chose `America/New_York` as the default deliberately — no explicit signal either way in existing seed data, but it's this codebase's closest thing to a default market assumption (matches the SMS/checkout flow's general US-first framing). Verified via direct query: both pre-existing vendors (`Corner Sourdough`, `Green Valley Produce`) picked up the default automatically, no `NULL`s, no hand-written backfill needed.
- New `src/lib/timezone.ts`: `zonedWallTimeToUtc()` (wall-clock string + IANA zone → UTC `Date`) and `utcInstantToZonedDatetimeLocal()` (the inverse, for the `min` attribute). Both hand-rolled with native `Intl` — no new dependency. `AddSlotForm.tsx`'s old local `toDatetimeLocalValue()` helper was removed, not left alongside the new module's equivalent, per the story's own "don't leave two divergent implementations" instruction.
- `AddSlotForm.tsx` now takes a required `timezone: string` prop (threaded from `dashboard/pickups/page.tsx`'s already-fetched `vendor.timezone`) and interprets its `datetime-local` inputs in that zone, not the browser's. Wire format to `POST /api/pickup-slots` is byte-identical to before (still an absolute ISO instant) — confirmed by re-running the full existing `pickup-slots/schema.test.ts` suite unmodified and green.
- `formatPickupWindow()` gained a required third `timeZone` parameter. All 4 real call sites updated (`vendors/[slug]/page.tsx`, `dashboard/pickups/page.tsx`, and 2 in `cart/page.tsx` — see Debug Log) plus the pre-existing Vitest test (updated to pass the runtime's own resolved zone, keeping its original machine-timezone-agnostic intent unchanged).
- `GET /api/vendors/[vendorId]/pickup-slots` now also returns `timezone` (falls back to `"UTC"` for an unknown `vendorId`, never actually rendered in that case since `slots` is also empty). Documented in `docs/api-contracts.md`.
- `AC #1, #2, #3, #5`: all satisfied, verified — see Debug Log for the two implementation-detail deviations from the story's original plan (both reasoned, both documented in Task 3/5's own checkbox notes). `AC #4`: satisfied for the dashboard listing (e2e-verified) and the storefront banner + cart page (unit + type-safety verified, not e2e — a deliberate, reasoned narrowing, not an oversight).
- Docs re-checked per Task 6: `docs/data-models.md` (`Vendor.timezone` row), `docs/api-contracts.md` (both the `POST /api/pickup-slots` comment and the `GET /api/vendors/[vendorId]/pickup-slots` contract, the latter for the unplanned `cart/page.tsx` fix).
- **Dev Notes' discretionary-scope disclosure requirement (explicit): `POST /api/admin/vendors` / `CreateVendorSchema` was *not* extended to accept a `timezone` at vendor-creation time.** Every vendor still gets the schema default regardless of how they're created. This is within the story's own stated discretionary scope (Dev Notes: "it is discretionary, not required by any AC here"), not a gap — recorded here per that same paragraph's explicit instruction to declare a skip rather than omit it silently (code review, round 1, caught this wasn't declared here originally).
- Full regression (pre-review-round): typecheck clean, lint clean, 108/108 unit, 137/137 e2e, production build succeeds. **Post-review-round (see Review Findings above for what changed):** typecheck clean, lint clean, 123/123 unit (+15 new), 137/137 e2e, production build succeeds.

### File List

- `prisma/schema.prisma` (modified — `Vendor.timezone`)
- `prisma/migrations/20260827172107_add_vendor_timezone/migration.sql` (new)
- `src/lib/timezone.ts` (new; **patched, round 1** — two-pass offset resolution replacing the buggy single-pass algorithm, round-trip validation throwing `InvalidWallTimeError`, input-shape validation moved inside the module, `isValidTimeZone()` added, `hourCycle: "h23"` replacing the two divergent `hour === "24"` workarounds, `utcInstantToZonedDatetimeLocal()` falls back to `"UTC"` for an invalid zone)
- `src/lib/timezone.test.ts` (from the ATDD pass; **patched, round 1** — 15 new cases: 8 pinned inside the previously-broken DST windows, 1 nonexistent-time, 1 ambiguous-time, 2 malformed-input, 2 `isValidTimeZone`, 1 `utcInstantToZonedDatetimeLocal` fallback)
- `src/lib/utils.ts` (modified — `formatPickupWindow()` gained a required `timeZone` parameter; **patched, round 1** — falls back to `"UTC"` for an invalid zone instead of throwing)
- `src/lib/utils.test.ts` (modified — existing test updated to pass the runtime's resolved zone, 1 new test proving the parameter changes output; **patched, round 1** — 1 new test for the invalid-zone fallback)
- `src/components/dashboard/AddSlotForm.tsx` (modified — `timezone` prop, `zonedWallTimeToUtc`/`utcInstantToZonedDatetimeLocal` call sites, removed the now-redundant local `toDatetimeLocalValue()`; **patched, round 1** — removed the separate shape-only regex guard, both conversion calls moved inside a `try`/`catch` that was previously only wrapping the network call)
- `src/app/dashboard/pickups/page.tsx` (modified — passes `vendor.timezone` to `AddSlotForm` and `formatPickupWindow`)
- `src/app/vendors/[slug]/page.tsx` (modified — passes `vendor.timezone` to `formatPickupWindow`)
- `src/app/cart/page.tsx` (modified, not originally planned — `vendorTimezone` state, passes it to both `formatPickupWindow` call sites; **patched, round 1** — runtime type + `isValidTimeZone()` check on the fetched `timezone` field instead of a bare type-cast, reset to `"UTC"` on `vendorId` change)
- `src/app/api/vendors/[vendorId]/pickup-slots/route.ts` (modified, not originally planned — response now also includes `timezone`; **patched, round 1** — the vendor lookup and slots query now run concurrently via `Promise.all` instead of sequentially)
- `tests/helpers/db.ts` (modified — `createTestVendor()` gained a `timezone` override; **patched, round 1** — idiom matched to its siblings)
- `tests/dashboard.spec.ts` (modified — 1 new e2e test proving AC #2/#4 end-to-end; **patched, round 1** — the test's target date is now computed relative to "now" instead of hardcoded, its `waitForResponse` now asserts success, and 2 other tests' stale comments referencing the removed `toDatetimeLocalValue()` were reworded)
- `docs/data-models.md` (modified — `Vendor.timezone` row)
- `docs/api-contracts.md` (modified — `POST /api/pickup-slots` comment, `GET /api/vendors/[vendorId]/pickup-slots` contract)
- `_bmad-output/implementation-artifacts/deferred-work.md` (modified — 6 findings deferred from this review round)

## Change Log

- 2026-08-27: Implemented Story 6.1 in full. `Vendor.timezone` schema migration (backfilled automatically, both existing vendors verified). New `src/lib/timezone.ts` (hand-rolled `Intl`-based wall-clock⇄UTC conversion, no new dependency) implemented against the ATDD pass's pre-existing 11 red tests, all now green. Threaded through `AddSlotForm.tsx` (slot creation) and `formatPickupWindow()` (display) — the latter's grep-every-call-site discipline surfaced 2 unplanned `cart/page.tsx` call sites, fixed in scope with a corresponding `GET /api/vendors/[vendorId]/pickup-slots` API-contract addition. The end-to-end AC #2 test used a different mechanism than originally planned (mutating the seeded vendor's timezone rather than a second Clerk-less fixture), documented with reasoning. Storefront-banner e2e coverage deliberately narrowed to unit + type-safety level, also documented with reasoning. Full regression: typecheck clean, lint clean, 108/108 unit, 137/137 e2e (zero regressions), production build succeeds. Status → review.
- 2026-08-27 (code review): 3-layer adversarial review on Opus (Blind Hunter, Edge Case Hunter, Acceptance Auditor), all three independently finding and reproducing by execution the same core defect: `zonedWallTimeToUtc()`'s single-pass offset lookup was wrong for a several-hour window around every DST transition in the schema's own default zone — the original DST tests all happened to sample a time outside that window, so they passed against a broken implementation. **Fixed (14 patches):** two-pass offset resolution; explicit rejection of a nonexistent (spring-forward-gap) wall time; a documented, pinned policy for an ambiguous (fall-back-repeated-hour) wall time; input-shape validation moved inside `zonedWallTimeToUtc()` (the prior claim that malformed input "throws" was itself wrong — it silently produced a year-2000 date); `AddSlotForm.tsx`'s conversion calls moved inside its `try`/`catch` (they were previously outside it, so a thrown error left the form permanently stuck on "Saving…"), which also let a redundant separate regex guard be removed; new `isValidTimeZone()` guarding `formatPickupWindow()`/`utcInstantToZonedDatetimeLocal()` against an invalid `Vendor.timezone` value crashing a public Server Component route; `cart/page.tsx`'s fetched `timezone` field now runtime-checked instead of blindly cast, and reset on vendor change; `GET /api/vendors/[vendorId]/pickup-slots`'s two queries now run concurrently; the new e2e test's hardcoded future date replaced with one computed relative to "now," and its `waitForResponse` now asserts success; two stale comments referencing a deleted helper reworded; a test fixture's inconsistent idiom matched to its siblings; a duplicated `hour === "24"` workaround consolidated via `hourCycle: "h23"`; this story's own Dev-Notes-required discretionary-scope disclosure added to Completion Notes (it existed in a doc file, but not where explicitly instructed). **Deferred (6, logged in `deferred-work.md`):** the product-level consequences of defaulting every vendor to `America/New_York` with no settings UI (both the going-forward and the retroactive-display angles), no timezone label on the customer-facing display string, the `min` attribute's blocking-constraint UX mismatch for a vendor whose browser zone differs from their configured one, the new e2e test's residual (undegraded, pre-existing-class) cross-file state-mutation risk under full parallelism, and the pre-existing (not introduced here) missing `deletedAt` filter on the pickup-slots route's vendor lookup. **Dismissed (2):** a theoretical `formatToParts()`-missing-part-key robustness gap with no known reproduction path, and a seconds-bearing-input regex concern that's unreachable given this form's inputs have no `step` attribute. Full regression re-run clean after all patches: typecheck, lint, 123/123 unit (15 new), 137/137 e2e (two individually-confirmed pre-existing-flakiness failures during repeated full-suite runs, both on unrelated tests, both clean in isolation and on a subsequent full clean run), production build succeeds. Status remains → review, pending Jeff's read of the 6 deferred items.
