# generated: 2026-08-17
# last_updated: 2026-08-25 (Story 5.1 created)
# project: local-food
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-08-17
last_updated: 2026-08-25 (Story 5.1 ready for review)
project: local-food
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-1: done
  1-1-verify-cart-line-removal-and-total-accuracy: done
  1-2-stock-quantity-captured-at-creation-backfilled-for-existing-products: done
  1-3-out-of-stock-products-are-marked-and-blocked: done
  1-4-inventory-decrements-immediately-on-sale-completion: done
  1-5-cart-quantity-stepper: done
  1-6-vendor-notified-of-placeholder-stock-quantity: done
  epic-1-retrospective: done

  epic-2: done
  2-1-admin-identity-and-access-gating: done
  2-2-admin-adds-a-vendor: done
  2-3-admin-deactivates-a-vendor: done
  epic-2-retrospective: done

  epic-3: done
  3-1-admin-inventory-dashboard: done
  3-2-low-stock-sms-alert-to-admin: done
  epic-3-retrospective: done

  epic-4: done
  4-1-vendor-uploads-a-product-image: done
  4-2-product-image-displays-on-the-storefront: done
  epic-4-retrospective: done

  epic-5: in-progress
  5-1-customer-selects-a-pickup-slot-at-checkout: review
  5-2-pickup-slot-creation-rejects-a-past-start-time: backlog

action_items:
  - epic: 1
    action: "Fix the stale Clerk vendor auth fixture (playwright/.auth/vendor.json) and build an equivalent Admin test-identity fixture before Epic 2's first story lands"
    owner: "Dev"
    status: done
    note: "Vendor half done 2026-08-21 - replaced with @clerk/testing Backend-API sign-in in playwright/support/global-setup.ts, regenerates automatically on every test run, no human/email step. Admin half was blocked only on credentials, not code - global-setup.ts's Admin branch existed since 2026-08-21 but had never run. 2026-08-23: E2E_ADMIN_EMAIL/E2E_ADMIN_CLERK_ID provisioned (see epic-3 action item), admin.json fixture now generates and signs in successfully."
  - epic: 1
    action: "Make npm run test:e2e:auth-style fixture generation self-sufficient (currently needs a dev server already running standalone)"
    owner: "Dev"
    status: done
    note: "Superseded rather than patched - the old script was deleted entirely in favor of Playwright globalSetup, which already runs inside the standard playwright test lifecycle (webServer included), so the two-terminal problem no longer exists."
  - epic: 1
    action: "Verify Epic 2's migration against a real DB (or rolled-back transaction) before the first review round, given Story 1.2's precedent of shadow-DB/timezone/cardinality bugs only catchable that way"
    owner: "Dev"
    status: done
    note: "All three Epic 2 migrations were simple (additive table; nullable-constraint relax + FK; two nullable columns + two onDelete changes) and were applied directly against the real shared dev DB as standard practice, each verified per its Dev Agent Record. Outcome achieved (no migration-shape surprises reached review), though not elevated into its own deliberate pre-review gate."
  - epic: 1
    action: "Before shipping any new interactive UI in Epic 2, check for touch/keyboard/screen-reader-reachable equivalents of any title-only tooltip content - this gap shipped twice in Epic 1 (Stories 1.5, 1.6)"
    owner: "Dev"
    status: done
    note: "No title-only-tooltip pattern shipped anywhere in Epic 2 - DeactivateVendorButton and AddVendorForm both used aria-label/role=\"alert\" from the start. Not re-verified as its own explicit checklist step, but the gap didn't recur."
  - epic: 1
    action: "Add npm run test:unit as a CI step (flagged in Story 1.2's round-2 review, still open)"
    owner: "Dev"
    status: done
    note: "2026-08-22: added to .github/workflows/ci.yml between lint and Playwright install. See Epic 3 action item of the same name for detail."
  - epic: 2
    action: "Provision the real E2E_ADMIN_EMAIL/E2E_ADMIN_CLERK_ID Clerk test user and GitHub Actions secrets - carried from Epic 1's retro, now blocking real e2e verification on 3 stories' worth of admin-authenticated tests"
    owner: "Jeff"
    status: done
    note: "2026-08-23: see epic-3 action item of the same name for detail."
  - epic: 2
    action: "Fix the ATDD scaffold-activation script's blanket test.skip( -> test( replacement - hit and fixed twice (Stories 2.2, 2.3) without the underlying method changing"
    owner: "Dev"
    status: done
    note: "Zero activation-script mistakes in either Story 3.1 or 3.2 - both used precise line-targeted sed, confirmed via grep before editing."
  - epic: 2
    action: "Add a lightweight doc-accuracy self-check to the end of implementation, before requesting review - doc drift has been a review finding in every single round across both epics"
    owner: "Dev"
    status: done
    note: "2026-08-22: codified as a durable rule in project-context.md. See Epic 3 action item of the same name for detail."
  - epic: 2
    action: "Treat check-then-act on shared mutable state as an implementation-time red flag, not just a review-time catch - this exact bug class shipped in 2 of Epic 2's 3 stories despite Epic 1 naming it explicitly"
    owner: "Dev"
    status: done
    note: "2026-08-22: codified as a durable rule in project-context.md, generalized to cross-request/cross-order atomicity. See Epic 3 action item of the same name for detail."
  - epic: 2
    action: "Decide on a fail-closed requireAdmin() helper before Story 3.2 (first admin action with a real external side effect)"
    owner: "Jeff"
    status: done
    note: "2026-08-23: see epic-3 action item of the same name for detail."
  - epic: 2
    action: "Deactivated vendor can still create Product/PickupSlot rows via their own untouched dashboard (logged in deferred-work.md from Story 2.3's review) - pick up when a story next touches vendor-dashboard write paths"
    owner: "Dev"
    status: done
    note: "2026-08-22: fixed directly (not as part of a story) - added assertVendorActive()/VendorDeactivatedError guards to POST /api/products and POST /api/pickup-slots, mirroring the existing checkout/storefront pattern exactly. Typecheck, lint, full unit suite (80/80), full e2e suite (100/114, 14 expected admin-credential skips), and production build all verified clean. No e2e test proves this end-to-end - would require a dedicated throwaway vendor Clerk identity, which doesn't exist yet (same class of gap as the admin e2e credential item, external/Jeff-owned); documented honestly in project-context.md's new Actual State Log rather than left unstated."
  - epic: 3
    action: "Provision the real E2E_ADMIN_EMAIL/E2E_ADMIN_CLERK_ID Clerk test user and GitHub Actions secrets, or make an explicit decision to accept the gap permanently now that no further admin-gated epic is currently planned - carried 3 epics running"
    owner: "Jeff"
    status: done
    note: "2026-08-23: created a 2nd Clerk Development-instance test user (distinct from the vendor identity), bound to a seeded Admin row via prisma/seed.ts, set as E2E_ADMIN_EMAIL/E2E_ADMIN_CLERK_ID in both .env and GH Actions repo secrets. All 21 previously-always-skipped admin-authenticated e2e tests ran for the first time; caught and fixed one real bug along the way (admin-vendors.spec.ts:110's getByRole('alert') collided with Next.js's route-announcer div - scoped to the form). Full suite now green: 80/80 unit, 114/114 e2e."
  - epic: 3
    action: "Add npm run test:unit as a CI step - open since Story 1.2's round-2 review, now 3 epics old"
    owner: "Dev"
    status: done
    note: "2026-08-22: added as its own CI step between lint and Playwright install, so a unit-test failure blocks e2e from running. Verified locally (80/80 green) before wiring in."
  - epic: 3
    action: "Make a real decision on the requireAdmin() fail-closed helper, even retroactively - decide whether to retrofit or explicitly accept the current fail-open-by-convention design"
    owner: "Jeff"
    status: done
    note: "2026-08-23: decided to close the structural gap at the routing layer rather than adding a per-route helper (which wouldn't have closed 'a route forgot to call it'). src/middleware.ts now gates /admin(.*) and /api/admin(.*) for 'must be signed in' before any route code runs - /api/admin(.*) wasn't covered by middleware at all before this. Per-route getCurrentAdmin() checks (src/lib/admin.ts) still do the actual Admin-role check, unchanged - Prisma can't run in Next 14.2.13's Edge middleware runtime, so a full DB-backed structural gate wasn't available without a bigger prerequisite change (Next 15 Node middleware, or moving admin-role into Clerk publicMetadata as a second source of truth) - both rejected as out of scope for this decision. Caught and fixed a real behavior gap along the way: auth().protect()'s default signed-out response is a redirect to the sign-in page, which returned 404 for unauthenticated JSON API callers (Clerk dev-browser rewrite target doesn't exist as a route) instead of the 401 every admin route handler already contracts for - middleware now returns a matching 401 JSON body for /api/admin(.*) directly instead of calling protect(). Verified: lint, typecheck, production build, 80/80 unit, 114/114 e2e all clean."
  - epic: 3
    action: "Strengthen the doc-accuracy self-check beyond scoping doc tasks narrowly - e.g. a diff-against-claims pass - since two stories running the current process still leaked real content errors into review"
    owner: "Dev"
    status: done
    note: "2026-08-22: codified as an explicit rule in project-context.md's Critical Don't-Miss Rules - re-read every touched doc line against the actual code before requesting review, not just scope the doc task narrowly. Durable, applies to every future story."
  - epic: 3
    action: "When implementing any invariant enforced only within a single transaction or single request, explicitly ask whether it holds under two different concurrent orders/requests hitting the same code path, not just one"
    owner: "Dev"
    status: done
    note: "2026-08-22: codified as an explicit rule in project-context.md's Critical Don't-Miss Rules, naming both the Epic 2 check-then-act races and the Epic 3 cross-order lowStockAlerted race as the two prior instances. Durable, applies to every future story."
  - epic: 3
    action: "Since epics.md's three-epic roadmap is now fully delivered, decide whether to groom a new epic or treat this roadmap as complete"
    owner: "Jeff"
    status: done
    note: "2026-08-24: Jeff's 4 scenarios (scenarios from Jeff/Local Food Scnearios from Jeff.rtf) reviewed against actual code, expanded via bmad-review-edge-case-hunter. Drafted and Jeff-approved into epics.md: Epic 4 (Vendor Product Images, FR14) and Epic 5 (Pickup Slot & Order Integrity, FR15/FR16) - 4 new stories total. Two scenarios (vendor delete+re-add, ASCII punctuation) confirmed already handled by existing code, no new epic; one small adjacent gap (accented-name handling) and one open decision (vendor-storefront timezone concept) logged as new deferred-work.md entries, both still awaiting a separate Jeff decision but not blocking roadmap continuation. Roadmap decision made: not complete, grooming continues with Epics 4-5."
  - epic: 4
    action: "Provision CLOUDINARY_* secrets in CI (.github/workflows/ci.yml), or explicitly accept the gap the way the admin-credential gap was eventually named and tracked - 2 of Epic 4's e2e tests have never run in CI"
    owner: "Jeff"
    status: open
  - epic: 4
    action: "When patching a code-review finding, re-run the full regression suite (not just the touched test file) before considering the patch done - Story 4.1's regex-stack-overflow bug would have shipped past a narrower check"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Before importing any constant from a route's schema.ts (or similar server-side module) into a \"use client\" component, check whether it reads process.env - a non-NEXT_PUBLIC_-prefixed value silently becomes undefined in the browser bundle with no build error"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Consider whether create-story should cite doc section headings instead of raw line numbers for any file not being edited in the same commit as the story - line references have now drifted stale twice across two epics (misattribution in 3.1, wrong-field citation in 4.1)"
    owner: "Jeff"
    status: open
