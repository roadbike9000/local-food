---
scope: local-food Admin & Inventory Expansion (PRD: prd-local-food-2026-08-10)
purpose: Fix invariants for admin role, vendor soft-delete, and inventory tracking so downstream stories don't diverge on shared-data shape or state-mutation paths
altitude: initiative->features (feature-level spine augmenting existing brownfield app)
updated: 2026-08-11T19:29
---

- (decision) [ADOPTED] Paradigm: Next.js 14 App Router, Server Components default, brownfield-ratified from existing codebase and project-context.md. No paradigm change.
- (direction) Mode: Coaching. Deliverable: spine only, no extra artifact.
- (decision) Admin identity: new Admin table keyed by clerkUserId, mirrors Vendor's existing pattern. Binds: any admin-gated route/action. Prevents: two call sites disagreeing on where admin-ness lives (DB vs Clerk claim).
- (decision) Stock model: isAvailable dropped as stored column, computed as stockQuantity > 0 wherever read. Binds: all storefront/checkout/cart queries. Prevents: isAvailable and stockQuantity drifting out of sync via a missed write path. Requires rewriting existing isAvailable:true filters in checkout route + storefront listings to filter on stockQuantity.
- (decision) [ADOPTED] Stock Quantity has exactly one write path: server-only adjustStock() function, conditional UPDATE WHERE stockQuantity >= delta, never a bare prisma.product.update from a route handler. Binds: FR-8 decrement, FR-10 alert-flag toggle, any future manual admin stock edit. Prevents: independent call sites racing on the last unit.
- (decision) [ADOPTED] Vendor-deactivated check goes through one shared guard (assertVendorActive()), not re-implemented per route. Binds: storefront route, checkout API, any future vendor-scoped route. Prevents: routes disagreeing on what 'deactivated' means.
- (decision) [ADOPTED] Admin attribution (FR-2) is a plain field (e.g. Vendor.deletedByAdminId, Vendor.createdByAdminId), not an audit-log table. Matches hobby/solo stakes.
- (decision) [ADOPTED] Admin pages live under new /admin/* route tree, separate from vendor /dashboard/*. New routes added to middleware.ts isProtectedRoute matcher (inherited rule). Each admin page/route calls new getCurrentAdmin() helper, mirroring existing getCurrentVendor() pattern -- middleware proves authenticated only, not admin. Binds: all new admin routes. Prevents: an admin route shipping unprotected or re-implementing the admin check ad hoc.
- (event) Reviewer gate run: lint clean; 4 parallel lenses (reconcile-PRD, rubric, tech-verify, adversarial) found convergent gaps: AD-2 checked availability not sufficiency (FR-7 oversell risk), AD-3 flag-flip timing contradicted FR-10's confirm-before-mark rule, AD-4 no throw/return contract, AD-2 missed a 3rd isAvailable call site (dashboard/products), FR-3 slug-collision had no AD, cascade-removal not in Structural Seed, admin-created Vendor clerkUserId binding unspecified, FR-11 stock-ceiling ownership unclear, multi-item decrement transaction semantics unspecified, AD-5 FK target unspecified, isAvailable same-pattern-different-name loophole, migration sequencing unordered.
- (decision) Admin-created Vendor login binding: Vendor.clerkUserId becomes nullable (schema change). Admin creates unbound Vendor record; binding to a real Clerk login happens manually/out-of-band later (no invite/claim flow built - that would exceed FR-3's literal scope). New AD-8.
- (event) All reviewer-gate findings resolved: tightened AD-2 (sufficiency check + no-cached-boolean loophole + migration order), AD-3 (transaction semantics + lowStockAlerted timing fix + client-ceiling as UX-hint), AD-4 (throw/return contract), AD-5 (FK target), added AD-7 (slug collision), AD-8 (vendor clerkUserId nullable, manual binding - user confirmed manual over invite-flow). Structural Seed + Capability Map updated to match. Lint clean. Spine finalized.
- (event) spine finalized
- (decision) New FR-13 (vendor placeholder-count dashboard notification) added post-finalization. New AD-9: PLACEHOLDER_STOCK_QUANTITY defined once, shared by migration script and dashboard banner check - prevents the two hardcoding different magic numbers. Binds/Capability Map/Consistency Conventions updated. Lint clean.
- (event) Sync fix: removed stale Deferred item for FR-12 backfill default (resolved as 100/0 during spec update, was never removed here).
- (event) Gap found during story writing: Admin table had no phone field, but FR-10 needs one to actually send the SMS. Added Admin.phone to ERD, mirrors Vendor.phone. Missed by original reviewer gate.
